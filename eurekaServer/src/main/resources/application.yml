eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    appname: eureka-server
  server:
    enable-self-preservation: false #关闭自我保护
    eviction-interval-timer-in-ms: 2000 # 剔除 已下线服务

server:
  port: 8761

spring:
  application:
    name: eureka-server